# Web 阶段工作约定

在完整科研工作区内遵循上级项目约定；独立克隆本仓库时，上级科研模块不随代码提供。此处维护网站阶段要求。Web代码、设计和服务数据构建产物在 `Web/` 内维护；上游科学规则与产物仍由负责模块维护。当前进度见 [README.md](README.md)。

## 目标与数据边界

- 基于已确认的科研数据，确定网站服务数据的组织、查询与呈现方式：每部分解决什么问题、如何表达实体及其关系。涉及上游实体定义或科学关系调整时，回到负责模块维护。
- 科研数据保存来源、科学含义、证据及已确认的整理结果；Web数据从科研正式产物中选择记录和字段。每项Web数据明确输入快照、粒度、关联键、字段、筛选条件及来源回溯。
- 分别确定数据库保留字段、API返回字段和页面展示字段；页面隐藏不等于API不返回，展示精简不删除上游正式数据。查询、关联和溯源所需字段可保留为内部字段。
- 分页、排序、字段投影和按已确认口径计数可在Web层实现；改变收录范围、证据含义、代表项选择、阈值或跨来源合并语义的处理，由上游模块确认并维护，不能隐含在页面或Web构建代码中。
- 展示保留实验与预测、缺失与真实零、身份关联与位点映射的区别；具体科学规则引用上游，不重复维护。

## 审查视角与分工

| 视角 | 职责 |
| --- | --- |
| 科研数据分析 | 明确研究问题、内容、实体与关系的科学含义，以及科研数据到Web数据的筛选依据 |
| 软件工程 | 设计数据库表组织、查询与接口，负责前后端、交互与UI、性能及实现验证 |

两种视角共同审查数据组织与方案，可由同一执行者完成，是否分工按任务需要决定。尚未确认的科学取舍与用户讨论；已授权的常规实现自主推进。

## 工作流程与验证

- 新板块原则上按分析、方案、必要的Demo、实现与验证推进；已确认范围内的修复和小改动直接执行，不要求每次完整重走流程。仅对影响科学含义、范围或关键交互的未决事项讨论确认。
- 分析先读相关模块契约、结果和必要样本，明确可用数据与缺口；方案写清研究问题、展示形式、实体关系、筛选规则和待决事项，区分建议与已确认决定。
- Demo优先使用可追溯的真实小样本，验证数据选择、查询/API和页面的一条完整链路；模拟数据明确标识。静态交互样稿可用于局部讨论，但不替代数据链路验证。
- Web构建绑定明确的上游快照和配置，通过可重复运行的入口生成；不修改上游正式产物，不静默混用不同版本。正式入库遵循总项目安排；入库方案明确失败处理和安全替换方式，不以保留历史数据版本为前提。
- 验证覆盖受影响的数据关系、接口和关键用户流程；涉及大表查询时，使用接近实际规模的数据验证分页、响应时间和资源占用。小样本Demo通过不代表全量性能可用；只做与本轮变更直接相关的必要验证并记录限制。
- 允许按证据迭代；未决问题只阻塞依赖它的步骤，不重复确认已有授权。

## 目录与数据维护

- 科研正式数据由 `modules/*/data/curated/` 维护；`Web/data/tables/` 保存最终选定、整理并验证后用于导入PostgreSQL的当前服务表，包含后端关联、查询及溯源需要的隐藏字段。数据库实际存储位置由部署配置指定。
- Web不采用 `releases/` 或 `runs/` 组织版本。优化后直接更新当前方案、脚本和数据；数据先写临时文件，必要核对通过后替换当前文件，不因构建失败覆盖可用产物。当前输入路径、来源自身版本、规则和构建时间仍须可追溯。
- `docs/plan/<NN_stage>/`按阶段维护当前计划与已采用的专题方案，注明要求/候选/采用状态及对旧基线的替代范围；`docs/plan/README.md`维护当前适用入口及沿用契约，不把待讨论目标冒充已确认方案。`docs/research/<NN_stage>/`使用同一阶段号保存问题、调研与待决事项，专题脚本、结果和图表随所属阶段维护。当前入口由Web README和`docs/README.md`维护，不再将不同阶段主题平铺在research或plan根目录。这是Web对上游滚动plan及runs约定的局部调整，不改变科研模块原有组织。
- `docs/record/<NN_stage>/`使用相同阶段号记录实际修改、产物、验证与限制；`docs/archive/<NN_stage>/{research,plan}/`归档旧阶段研究及方案基线，保留依据和资源。归档不撤销未被替代的规则，其适用入口由plan索引明确；归档也不代表待决问题已经解决。移动时同步相对引用和必要脚本路径，在record说明；不要复制数据或把旧记录的授权状态当作当前指令。阶段号从本次01开始，此前统一归00；仅实际进入下一阶段才新建编号，不为每次聊天预建目录。
- 正式构建与入库代码放 `src/`，与调研脚本分开；可复用逻辑只维护一份，调研脚本可调用正式代码。
- `docs/history/` 用MD记录重要历史决定、被替代方案和上下文：问题、旧决定、新决定、改变原因、依据及影响；明确链接当前方案。不为每次编辑备份文档，不保存聊天流水账，不要求保留历史数据副本，也不将历史MD视为旧产物可精确恢复的保证。
- 具体目录布局见[README](README.md#目录布局与归档)，按实际任务创建，不预建空目录或空文档。

## 精简维护

`AGENTS.md`维护稳定约定，`README.md`维护入口与当前阶段。字段目录、实体关系、API设计和验收案例按实际需要进入相应设计文档，不预建空文档。同一事实只维护一处，其他位置链接；不抄聊天记录，不重复上级规则。沿用项目已确认的技术决策（包括PostgreSQL）；尚未确定的技术栈按需求选择，确定后在设计文档维护。

## GitHub 版本基线

2026-09-22用户确认当前网站版本并授权发布至 https://github.com/PuppetZhou/memVar 。本仓库根目录对应科研工作区的 Web/；后续修改基于当前 main 提交，保留必要验证记录。数据、凭据、调研导出和运行产物不上传；首次发布前的本地历史备份分支不推送。
