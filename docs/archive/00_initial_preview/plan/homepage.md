# 首页与分类检索

> 00阶段方案基线，2026-09-22归档。保留原方案与当时状态；适用范围、01阶段修订及未替代契约见[现行方案索引](../../../plan/README.md)。旧阶段授权/待执行文字不作为当前指令。

2026-09-21；用户授权入口页面首版设计与实现。当前为可运行首版，视觉方案仍可根据用户反馈调整。

- `/` 展示大搜索栏、交互式数据层总览、膜蛋白分类、膜定位、代表案例；不再跳转 EGFR。
- 搜索覆盖现有蛋白名称、基因名及 UniProt 身份字段；分类卡片进入 `/search?membrane_class=…`，可继续文字搜索和数字分页，URL 保存筛选状态。
- 分类直接使用 `web.protein_membrane_label` 的四个现行标签，不派生新的跨膜次数分类；标签允许重叠。总蛋白数与分类数实时读取服务库；分类按 accession 去重。
- 代表入口为 EGFR/P00533、KCNH2/Q12809、AQP4/P55087；分别引导受体信号、离子通道、水转运相关信息浏览，不保证每种证据均覆盖每个条目。
- 工作视觉方案：白色背景、深蓝文字、royal blue 搜索按钮，分类区域白底；保留用户绿色蛋白/灰色膜分类插图。首页搜索区使用蓝色蛋白景观背景，支持脂质双层、蛋白景观与绿色丝带三种手动切换；数据总览使用浅蓝—淡紫底和七层语义色。桌面四列分类、三列案例；小屏垂直重排。搜索、卡片、链接均为原生可访问控件。
- 验收：真实分类计数、组合搜索、数字分页、空结果、案例路由、桌面及移动版无横向溢出。

调研与视觉核对见 [首页调研](../research/homepage/design.md)。不修改数据库、科学整理规则或已有蛋白详情模块。

## 素材与膜定位补充（2026-09-21）

- 用户原图保留在 `Web/docs/ChatGPT Image 2026年9月21日 05_54_02.png`。网站素材副本在 `frontend/public/images/home/`；三类示意经图像工具整理为四格素材，第四格为新绘 Membrane-related 符号。图片为示意，不作为真实蛋白结构或类别判定依据。
- 新增按膜定位浏览：现行 `web.protein_uniprot_location` 中 `scope_type=entry`、原始 location 名称含 membrane 且具有SL标识符的定位；按原始ID精确筛选，不进行父子定位推断或归并，不将isoform/unresolved注释提升为entry证据。
- 首页展示数量最多的8个具体定位，泛称Membrane保留在全部82项下拉菜单并标记unspecified。相同蛋白可出现于多个定位；计数按accession去重，条形长度相对于首个定位数量，不代表表达水平。
- `/search?membrane_location=SL-0039` 等入口可与关键词、膜蛋白类型组合，切换条件重置分页。API增加membrane_location，不改变既有检索默认范围。

## 七层数据总览与背景更新（2026-09-21）

- 背景素材补充为同目录06.08.18和06.08.39两张用户截图。保留原件，内置图像工具去掉说明文字/箭头，整理为宽幅蓝色蛋白与脂质双层景观；默认全景，另提供双层局部和上一版绿色丝带。手动切换，不自动轮播，兼容减少动态效果偏好。
- 搜索后新增 `Discover what’s in memVar`：蛋白与序列、变异、预测、表达、调控、互作、疾病七个视图。点击更新精简内容预览与示例入口；不再展示来源分布条或长统计清单。保留分类、定位和案例。
- 复用 `/api/catalog/statistics`，不独立扫描大表、不复制统计快照。按服务端版本匹配结果显示当前统计；错误显示不可用，不回退为0或过期硬编码数字。
- 表达主数字改为该接口已列的13个数据集，不再把跨测量观测行数之和放在首页；该目录不包含GTEx median vectors，范围在按需说明中明确；工具数指命名工具/配置，不把61个分数字段称为61种独立算法。其余指标使用接口原始单位。
- 首页不再重复全库分布图或来源数量；七个入口各保留一个紧凑主数字及单位，精确值在可访问标签和About this count中保留。可重叠数据不生成混合层次饼图或跨层总量。
- 完整统计入口到 `/results`，按需计数帮助中可跳到对应章节；示例按钮进入EGFR的相应证据板块。保留已有异步hash定位。


## 首页精简概览（2026-09-21后续反馈，当前）

用户指出首页不应像Data Overview一样罗列来源统计。首页职责改为“有多大、能看什么、从哪里开始”：

- 七个横排彩色入口，每层一个主数字＋单位；选中态用浅底与下划线明确。窄屏重排，不要求横向滚动。
- 预览左侧短标题/一句用途/示例入口，右侧三个具名主题图标卡，不再显示来源柱图、下拉图形选择、长来源展开、重复问题和长计数脚注。
- Protein/Prediction/Disease仅保留一个有用辅助指标（序列、可选评分字段、表型注释）；其余层不为了对齐而补造指标。
- Expression从83,921,729观测行的首页主数字改为13个列示数据集。这是显示指标选择，不是改变收录或统计JSON。其余六层主数字及原单位保持不变。
- About this count按需解释精确值、计数基准、PPI与表达范围，完整统计跳Data Overview。点击Explore EGFR进入Sequence、Variant、Expression、QTL、Interaction或Disease实际锚点；首页不保证每种证据覆盖所有蛋白。
- 本轮只修改`HomeEvidence.tsx`及`home-evidence.css`；首页搜索、背景切换、膜分类/定位、案例和已完成的Data Overview图表保持现有功能。

构建、静态验证及浏览器限制见[交付追加](../../../record/00_initial_preview/20260921_homepage_v1.md#首页数据概览精简后续追加)。
