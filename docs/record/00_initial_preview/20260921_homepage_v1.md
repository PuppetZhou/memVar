# 首页第一版交付

2026-09-21，本地入口：http://127.0.0.1:8000/ 。

## 实现

- `/` 改为搜索优先首页；四类膜蛋白卡片和 EGFR/KCNH2/AQP4 案例可点击。
- 新增 `/api/catalog/summary` 读取总数与去重分类计数；`/api/proteins` 新增标签过滤、offset及total，保留原文字检索与精确匹配优先。
- 结果页可继续搜索、切换分类及数字翻页。筛选变更重置页码，浏览器URL保存条件；空页可回第一页。
- 保留已有蛋白页面；本次不修改数据库与科研数据。

主要文件：`frontend/src/components/HomePage.tsx`、`ProteinSearch.tsx`、`homepage.css`、`frontend/src/App.tsx`、`src/api/core.py`。

## 验证

- TypeScript及Vite构建通过。
- FastAPI TestClient连接当前只读库：四类计数与分类检索total一致；前两页去重且不交叉；EGFR/KCNH2/AQP4精确匹配排序通过；非法类别/负offset返回422；空结果与通配符转义通过。
- 独立Playwright：分类入口→5,213结果→第2页→EGFR组合搜索与页码重置→空结果，通过。
- 手机390×844：无横向溢出；AQP4案例进入真实蛋白详情。桌面1374×1145与手机截图已实际检查。
- 浏览器脚本首次因CLI沙箱不提供URL构造器中断；此前断言已通过，改用字符串检查后继续完成剩余验收。该错误不来自应用。
- 本地API服务重启加载新路由，健康检查返回PostgreSQL/read_only=true。

工作草图与最终页面差异、问题清单和来源依据见 [首页调研](../../archive/00_initial_preview/research/homepage/design.md)；当前方案见 [首页方案](../../archive/00_initial_preview/plan/homepage.md)。仅本地可运行首版，尚待用户视觉反馈；没有执行公网部署。

## 素材与膜定位迭代验收

- 用户素材已用于主页淡背景及三类插图，Membrane-related补同绿色符号；素材与提示词见调研文档。
- 新增膜定位区、全部82项菜单、`membrane_location`检索参数以及结果页位置筛选。仅读取已有UniProt entry字段，不改变数据库或上游科学规则。
- TypeScript/Vite构建通过；TestClient验证前10个定位计数与结果total一致、结果去重、EGFR+Cell membrane+Integral组合匹配及非法参数422。
- 实际浏览器：Cell membrane 3,517结果→第2页→EGFR搜索页码重置，通过；手机下拉选择Mitochondrion outer membrane得到150结果；桌面1374px、手机390px主页和结果页均无横向溢出。
- 实際截图查看通过：原图背景可辨而不遮挡正文；四类图保持比例；定位数量条与来源提示可读。现行截图更新于同一调研目录。

## 多层数据总览与背景切换迭代

2026-09-21；本轮为首页增补，不修改数据库或统计构建产物。

- 新增 `HomeEvidence.tsx` / `home-evidence.css`，七层切换复用现有 `/api/catalog/statistics`；不额外扫描大表。主指标、类别分布、精确值选择、完整来源列表、计数说明及完整统计入口可用。
- 基于两张新素材整理无字蓝色膜蛋白景观；默认Protein landscape，支持Lipid bilayer局部与Green ribbons手动切换。保留分类、定位与案例。
- 参考CATVariant实际渲染的浅蓝紫背景、导航胶囊和逐层展开；没有复制其统计值或自动分析功能。
- `DatabasePages.tsx` 的页内hash处理补上异步目标出现后的滚动，让Full coverage与source链接正确定位。保留并行新增的Data overview/Documentation和PageJump。

验证：

1. TypeScript/Vite构建通过。
2. Playwright七层分别选择；表达展开13个来源、预测展开43个配置、分布行选择及切换分布模式均通过。
3. 三种背景鼠标切换与Space键激活通过；1440px、390px无横向溢出；真实截图经图像查看工具检查。
4. Full coverage进入 `/results#variants` 后目标章节顶端位于视口0–200px，修复前在视口下方约2536px。
5. 模拟统计接口503：显示错误提示，不渲染带假零的层次卡片；解除模拟后恢复真实数据。测试中预期出现503网络日志，不是线上真实统计失败；正常七层/背景流程未捕获页面运行异常。
6. 前端文件已构建由本地服务提供；无需API服务重启。Browser运行时此前不可用，沿用已记录授权的独立Playwright会话，未操作主线程浏览器。

背景资产和完整提示词、数字口径及HOME-13至HOME-17问题见 [调研记录](../../archive/00_initial_preview/research/homepage/design.md)。最终 [主视觉](../../archive/00_initial_preview/research/homepage/hero.png)、[交互式数据总览](../../archive/00_initial_preview/research/homepage/evidence.png)、[完整桌面](../../archive/00_initial_preview/research/homepage/desktop.png)、[手机](../../archive/00_initial_preview/research/homepage/mobile.png)。


## 首页数据概览精简（后续追加）

2026-09-21：根据最新反馈，移除Home七层详情内来源柱图、图形选择器、长清单展开和重复介绍。新布局为七个数字入口＋短预览，三类主题图标卡、可选单一辅助指标、Explore EGFR和按需计数帮助；完整统计链接Data Overview。首页背景、搜索、分类、定位及其它模块未修改。

Expression首页数字改为13个已列数据集；其余仍为7,715蛋白、10,866,094变异、43工具/配置、251,055,619 QTL行、1,267,236 PPI存储记录、25,442疾病证据记录。精确值与单位可查看，不把不同单位相加。未修改API、统计JSON或数据库。

验证：

- TypeScript/Vite构建通过，1,702模块。
- 用真实统计响应对七层LayerPreview逐一做React静态渲染：原数字及Expression新指标正确，三个内容主题与EGFR目标链接齐全，无React渲染警告、NaN/Infinity。
- 整个HomeEvidence使用已填入真实统计的查询缓存渲染，七个选择入口、完整统计链接和listed datasets单位存在；旧柱图/Show all不再渲染。
- 目标锚点核对：sequence、variants、expression、qtl、interactions、diseases均与现有组件ID一致。
- 浏览器连接返回`No browser is available`；本次选择切换、帮助弹窗、示例跳转及桌面/手机实际布局待浏览器验收。早期截图和交互验收不代表本次改版已实测。

现行呈现规则维护于[首页方案](../../archive/00_initial_preview/plan/homepage.md#首页精简概览2026-09-21后续反馈当前)。
